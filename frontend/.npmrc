auto-install-peers=true
strict-peer-dependencies=false
# Mitigación frente a paquetes recién publicados (supply-chain attacks):
# rechaza versiones con menos de 7 días desde su publicación (10080 minutos).
minimum-release-age=10080
minimum-release-age-exclude=next,react,react-dom
